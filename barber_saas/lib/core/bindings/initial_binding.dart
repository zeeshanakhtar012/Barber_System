import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Core dependencies will be injected here
    // e.g., SharedPreferences, Firebase Auth Controllers
  }
}
